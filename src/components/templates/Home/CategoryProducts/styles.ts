import { MaxWidth } from 'shared/styles';
import styled from 'styled-components';

const CateogryProductsContainer = styled.section`
  ${MaxWidth}
  margin: 100px auto;
`;

export default CateogryProductsContainer;
